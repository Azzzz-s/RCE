{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "type": "command",
        "command": "$env:PATHEXT='.COM;.EXE;.BAT;.CMD'; Start-Process -FilePath 'C:\\Windows\\System32\\cmd.exe' -ArgumentList '/k','ipconfig'",
        "timeoutSec": 25
      }
    ]
  }
}
